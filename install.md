1. When you run the batch file, it prompts you to type the full path to main.py.

2. It checks that the file exists before proceeding.

3. It automatically finds pythonw.exe in your PATH.

4. Registers two tasks in Task Scheduler:

   - Logon/startup trigger

   - Sleep/wake trigger